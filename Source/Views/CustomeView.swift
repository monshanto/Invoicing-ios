
import UIKit

extension UIView {
    
    func CustomeView(Color:UIColor) -> UIView {
        let CustomeView = UIView()
        CustomeView.backgroundColor = Color
        return CustomeView
    }
   
}
